# Copy to `.env` in this folder (never commit `.env`).
# See root `.gitignore` — `.env` is ignored.

AI_PROVIDER=openai-compatible
AI_API_KEY=
AI_MODEL=gpt-4o-mini
# AI_BASE_URL=https://api.openai.com/v1/chat/completions
